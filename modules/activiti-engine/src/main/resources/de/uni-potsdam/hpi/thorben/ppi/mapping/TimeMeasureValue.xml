<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="de.unipotsdam.hpi.thorben.ppi.measure.entity.TimeMeasureValue">

  <!-- Write a measured value -->

  <insert id="insertTimeMeasur" parameterType="de.unipotsdam.hpi.thorben.ppi.measure.entity.TimeMeasureValue">
    insert into ${prefix}PPI_BASE_TIME_MEASURE (ID_, MEASURE_ID_, PROC_INST_ID, FROM_, TO_)
    values (
      #{id ,jdbcType=VARCHAR},
      #{measureId ,jdbcType=VARCHAR},
      #{processInstanceId ,jdbcType=VARCHAR},
      #{from ,jdbcType=VARCHAR},
      #{to ,jdbcType=VARCHAR}
    )
  </insert>
  
  <!-- USER RESULTMAP -->

  <resultMap id="measureResultMap" type="de.unipotsdam.hpi.thorben.ppi.measure.entity.TimeMeasureValue">
    <id property="id" column="ID_" jdbcType="VARCHAR" />
    <result property="measureId" column="MEASURE_ID_" jdbcType="VARCHAR" />
    <result property="processInstanceId" column="PROC_INST_ID" jdbcType="VARCHAR" />
    <result property="from" column="FROM_" jdbcType="VARCHAR" />
    <result property="to" column="TO_" jdbcType="VARCHAR" />
  </resultMap>
  
  <!-- USER SELECT -->

  <select id="selectMeasureById" parameterType="string" resultMap="measureResultMap">
    select * from ${prefix}PPI_BASE_TIME_MEASURE where ID_ = #{id,jdbcType=VARCHAR}
  </select>
  
  <!-- <insert id="insertToValue" parameterType="de.unipotsdam.hpi.thorben.ppi.measure.entity.TimeMeasureValue">
    insert into ${prefix}PPI_BASE_TIME_MEASURE (ID_, MEASURE_ID_, PROC_INST_ID, TO_)
    values (
      #{id ,jdbcType=VARCHAR},
      #{measureId ,jdbcType=VARCHAR},
      #{processInstanceId ,jdbcType=VARCHAR},
      #{to ,jdbcType=VARCHAR}
    )
  </insert> -->
</mapper>