<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="de.unipotsdam.hpi.thorben.ppi.measure.entity.TimeMeasureValue">

  <!-- Write a measured value -->

  <insert id="insertTimeMeasur" parameterType="de.unipotsdam.hpi.thorben.ppi.measure.entity.TimeMeasureValue">
    insert into ${prefix}PPI_BASE_TIME_MEASURE (ID_, MEASURE_ID_, PROC_INST_ID, FROM_, TO_)
    values (
      #{id ,jdbcType=VARCHAR},
      #{measureId ,jdbcType=VARCHAR},
      #{processInstanceId ,jdbcType=VARCHAR},
      #{from ,jdbcType=VARCHAR},
      #{to ,jdbcType=VARCHAR}
    )
  </insert>
  
  <!-- <insert id="insertToValue" parameterType="de.unipotsdam.hpi.thorben.ppi.measure.entity.TimeMeasureValue">
    insert into ${prefix}PPI_BASE_TIME_MEASURE (ID_, MEASURE_ID_, PROC_INST_ID, TO_)
    values (
      #{id ,jdbcType=VARCHAR},
      #{measureId ,jdbcType=VARCHAR},
      #{processInstanceId ,jdbcType=VARCHAR},
      #{to ,jdbcType=VARCHAR}
    )
  </insert> -->
</mapper>